# 教学智能工作台（教师 / 教研员 / 班主任 · 三合一）

依据两篇公众号文章整理构建的三合一教学 AI 工作台：
**《从备课到评课，20个WorkBuddy场景帮你搞定教学全链路》**（赛 / 训 / 教 / 研 / 评 / 管）
与 **《教师工作台》**（班级管理：今日班务、过关记录、考勤、家校、值日、排座、家长会）。

## 功能

- **三大角色工作台（各是一套卡片式工作台，一键切换）**：
  - 👩‍🏫 **教师工作台**（13 个场景）：磨课备赛、学情分析、教学设计、课堂复盘、反思自评、过关记录整理
  - 🔬 **教研员工作台**（10 个场景）：培训诊断与定制、同课异构、资源推送、赛事评课、成长画像、数据大屏
  - 📋 **班主任工作台**（12 个场景）：今日班务总览、过关记录、补测名单、成绩分析（无排名版）、考勤请假、家校沟通、值日排班、排座位、班务待办、班干部、学生档案、家长会材料
- **七大维度**：赛 / 训 / 教 / 研 / 评 / 管 / 班，共 32 个场景
- **🤖 AI 智能对话（内置大模型直连）**：在页面里直接连接任意 OpenAI 兼容接口（DeepSeek / 通义千问 / Kimi / 智谱 / OpenAI / 本地 Ollama），流式对话、上传材料直接处理
- **材料上传**：支持 Word（.docx）、PPT（.pptx）、Excel（.xlsx）文档自动解析成文本，以及 .txt/.md/.json/.csv 等文本文件（Office 解析基于本地 JSZip，无需联网）
- **场景一键装载**：场景详情点「用此场景开始 AI 对话」，AI 立即扮演对应角色，粘贴材料即可开工
- **搜索 / 收藏夹 / 最近使用 / 一键导出**全部提示词
- 每个场景内置**使用边界提醒**：AI 输出仅供参考、需人工审核；不做横向排名、不贴负面标签；家长联系方式等敏感信息不进汇总与打印

## 目录结构

```
├── index.html        # 入口页面
├── css/style.css     # 样式
├── js/data.js        # 32 个场景数据（角色 / 维度 / 提示词 / 边界）
├── js/markdown.js    # 迷你 Markdown 渲染器（AI 回复展示）
├── js/chat.js        # AI 对话模块（模型连接 / 流式对话 / 材料上传）
├── js/office.js      # Office 文档解析（docx / pptx / xlsx → 文本）
├── js/vendor/jszip.min.js  # JSZip 解压库（MIT 协议，本地离线）
├── js/app.js         # 交互逻辑（渲染 / 搜索 / 收藏 / 复制 / 导出）
└── README.md
```

## 使用方式

纯静态页面，无需构建：

1. 直接双击打开 `index.html`；
2. 或启动本地静态服务预览（推荐，剪贴板 API 更稳定）：

```bash
# 方式一：Python
python -m http.server 8000
# 浏览器访问 http://localhost:8000

# 方式二：Node
npx serve .
```

## 使用方法（三步）

1. **选场景**：按维度浏览或搜索，找到想省时间的任务；
2. **复制提示词**：打开场景详情，一键复制提示词；
3. **发给 AI**：粘贴到 WorkBuddy / DeepSeek / 文心一言 等对话式 AI，附上材料（教案、成绩表、课堂实录转写等）按提示词对话即可。

> ⚠️ 所有场景的 AI 输出仅供参考，关键结论请结合实际情况人工审核；注意保护学生与教师隐私，不做横向排名、不贴负面标签。

## 连接大模型（内置 AI 对话）

1. 打开页面 → 点击左侧 **🤖 AI 智能对话** → 右上角 **⚙️ 模型设置**；
2. 快捷选择服务商（DeepSeek 官方 / 通义千问 / Kimi / 智谱 / OpenAI / Ollama 本地），填入 API Key 与模型名称，点 **🔌 测试连接** 验证，再 **💾 保存并连接**；
3. 回到对话区直接输入，或 📎 上传教案 / 成绩表 / 课堂实录等文本材料让 AI 处理；
4. 更省事：打开任意场景卡片 → **「用此场景开始 AI 对话」**，AI 直接进入该角色。

> ⚠️ API Key 仅保存在本机浏览器 localStorage。若浏览器跨域（CORS）被拦截，可改用 **Ollama 本地模型**（免费、无需 Key），或自建 OpenAI 兼容网关（one-api / new-api）。
